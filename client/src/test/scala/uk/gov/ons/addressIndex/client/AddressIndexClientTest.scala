package uk.gov.ons.addressIndex.client

import javax.inject.{Inject, Singleton}
import org.scalatest.FlatSpec
import play.api.libs.ws.WSClient

class AddressIndexClientTest extends FlatSpec {
  ignore "no tests" in {
    assert(true)
  }
